[project]
name = "nexusai-api"
version = "1.0.0"
description = "NexusAI Security API"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
